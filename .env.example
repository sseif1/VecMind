# VecMind Environment Variables
# Copy this file to .env and fill in your actual values

# OpenAI API Key
# Required for generating embeddings. Get your key from https://platform.openai.com/api-keys
# If not provided, VecMind will use a fallback hash-based embedding (not semantic)
OPENAI_API_KEY=your_openai_api_key_here

# Database Connection URL
# Format: postgresql://user:password@host:port/database
# Default: postgresql://postgres:postgres@localhost:5432/vecmind
# If using Docker Compose, the default should work
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/vecmind
